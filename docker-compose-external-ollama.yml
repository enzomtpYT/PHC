version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-simple
    ports:
      - "8501:8501"
    environment:
      - OLLAMA_HOST=host.docker.internal
    restart: unless-stopped
